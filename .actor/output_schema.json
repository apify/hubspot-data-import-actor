{
    "actorOutputSchemaVersion": 1,
    "title": "HubSpot Import Results",
    "description": "Output from the HubSpot company data import operation",
    "properties": {
        "results": {
            "type": "string",
            "title": "Per-company Results",
            "description": "Individual import result for each company",
            "template": "{{links.apiDefaultDatasetUrl}}/items"
        },
        "summary": {
            "type": "string",
            "title": "Import Summary",
            "description": "Overall import summary with totals and counts",
            "template": "{{links.apiDefaultKeyValueStoreUrl}}/records/OUTPUT"
        }
    }
}
